# 📡 수집 시스템 연동 가이드

> nav-app(네비게이션)과 센서 수집 백엔드를 연결하는 방법을 설명합니다.  
> **처음 하는 분도 순서대로 따라 하면 됩니다.**

---

## 🤔 연동이 뭔가요?

차량 안에는 두 가지 시스템이 같이 돌아가고 있어요.

```
📱 태블릿 / 스마트폰 ←── 같은 Wi-Fi ──→ 💻 수집 PC (차량 내)
  (네비게이션 앱)                         (센서 녹화 프로그램)
```

이 둘을 연결하면:

| 따로 쓸 때 | 연결하면 |
|---|---|
| 운전자가 수동으로 녹화 버튼을 눌러야 함 | ▶ 시작 누르면 녹화 자동 시작 |
| 센서가 잘 되는지 모름 | 태블릿 화면에서 바로 확인 가능 |
| 문제가 생겨도 나중에 알게 됨 | 경로 이탈·센서 오류·디스크 꽉 참 → 디스코드로 즉시 알림 |

---

## ✅ 준비물 체크

연결하기 전에 아래 세 가지가 다 갖춰져 있는지 확인하세요.

- [ ] 차량 내 수집 PC가 켜져 있음
- [ ] 태블릿(또는 스마트폰)과 수집 PC가 **같은 Wi-Fi**에 연결되어 있음
- [ ] 센서 녹화 프로그램이 수집 PC에 설치되어 있음

> 💡 **같은 Wi-Fi란?**  
> 태블릿과 수집 PC 둘 다 차량 내 공유기(또는 핫스팟)에 연결되어 있어야 해요.  
> 태블릿이 LTE, 수집 PC가 Wi-Fi라면 연결이 안 됩니다.

---

## 🖥️ STEP 1 — 수집 PC에서 백엔드 실행하기

수집 PC에서 아래 명령어를 실행해주세요.

```bash
cd 센서 녹화 프로그램webui/backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

실행하면 이런 화면이 나와요:

```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

이 창을 **닫지 말고** 그대로 두세요. 닫으면 백엔드가 꺼집니다.

> ❓ **`--host 0.0.0.0` 이 뭔가요?**  
> 기본값(`127.0.0.1`)으로 실행하면 수집 PC 자기 자신만 접근할 수 있어요.  
> `0.0.0.0`으로 실행해야 같은 네트워크의 태블릿도 접근할 수 있습니다.

---

## 🔍 STEP 2 — 수집 PC의 IP 주소 확인하기

수집 PC의 IP 주소를 알아야 태블릿에서 접속할 수 있어요.

**Windows라면:**
```
윈도우 키 + R → cmd 입력 → 엔터
ipconfig
```

아래처럼 나오는 숫자가 IP 주소예요:
```
IPv4 주소 . . . . . . : 192.168.0.42   ← 이 숫자!
```

**Linux라면:**
```bash
ip addr show
# 또는
hostname -I
```

> 💡 **IP 주소는 보통 `192.168.xxx.xxx` 형태예요.**  
> `127.0.0.1`은 수집 PC 자기 자신을 가리키는 주소라 쓰면 안 돼요.

---

## 📱 STEP 3 — nav-app에서 연결하기

1. nav-app에서 우상단 ⚙️ 버튼 클릭
2. **📡 수집 시스템 설정** 클릭
3. 수집 PC 주소 입력란에 아래 형식으로 입력

```
http://192.168.0.42:8000
```

> ⚠️ `192.168.0.42` 부분을 STEP 2에서 확인한 실제 IP로 바꿔주세요.  
> `http://` 와 `:8000` 은 그대로 써야 해요.

4. 프리셋 선택 — 드롭다운에서 사용할 센서 프리셋 선택
5. **저장 및 연결** 클릭

연결에 성공하면 지도 **우하단**에 이런 패널이 나타나요:

```
● 수집 정상
```

---

## 🔔 STEP 4 — 디스코드 알림 설정하기 (선택)

문제가 생겼을 때 디스코드로 알림을 받을 수 있어요.

**알림이 오는 상황:**

| 상황 | 알림 내용 |
|---|---|
| 🚨 경로 이탈 | 차량이 경로에서 100m 이상 벗어났을 때 |
| ⚠️ 센서 이상 | 센서 Hz가 떨어지거나 데이터 드랍이 생겼을 때 |
| 💾 디스크 경고 | 수집 PC 저장 공간이 90% 이상 찼을 때 |

**설정 방법:**

1. 디스코드에서 알림을 받을 채널의 웹훅 URL을 복사해 두세요
   - 채널 설정 → 연동 → 웹후크 → 새 웹후크 → URL 복사
2. nav-app ⚙️ → **📡 수집 시스템 설정** 클릭
3. 하단 **Discord 웹훅 URL** 입력란에 붙여넣기
4. **저장 및 연결** 클릭

> 💡 웹훅 URL을 비워두면 알림 없이 모니터링만 됩니다.  
> 알림은 같은 종류당 최소 5분 간격으로 보내요 (도배 방지).

---

## 📊 모니터링 패널 보는 법

연결되면 지도 우하단에 패널이 항상 표시돼요. 탭하면 펼쳐집니다.

### 패널 색상

| 색상 | 의미 | 할 일 |
|---|---|---|
| 🟢 초록 `수집 정상` | 모든 센서 정상 작동 중 | 그냥 주행하면 됩니다 |
| 🟡 노랑 `센서 이상` | 일부 센서에 문제 있음 | 펼쳐서 어느 센서인지 확인 |
| 🔴 빨강 `연결 끊김` | 수집 PC와 통신 안 됨 | 아래 문제 해결 참고 |

### 펼쳤을 때 보이는 정보

```
● 수집 정상  ▼
─────────────────
경과: 12분 34초

● lidar_front    10.0 Hz
● camera_left    30.0 Hz
● camera_right   30.0 Hz

프리셋: default
```

- **Hz** : 초당 수신 횟수. 라이다는 보통 10Hz, 카메라는 30Hz
- **drop** : 숫자가 보이면 데이터 손실이 발생한 것 → 관리자에게 알리세요
- **경과** : 현재 구간 녹화 시작 후 지난 시간

---

## ▶ 실제 주행 중 동작

연결이 완료된 상태에서 주행하면 이렇게 자동으로 연동돼요.

```
운전자가 ▶ 시작 누름
    └→ nav-app이 녹화 시작 명령 전송
    └→ 센서 녹화 프로그램이 센서 녹화 시작

주행 중
    └→ nav-app이 1.5초마다 센서 상태 확인
    └→ 문제 발생 시 디스코드 알림 전송

운전자가 ⏸ 정지 누름
    └→ nav-app만 정지 (경로 안내·타이머 중단)
       센서 녹화 프로그램은 계속 돌아감 — nav-app과 무관하게 독립 동작
```
---

## 📋 연결 체크리스트 (매일 출발 전)

```
□ 수집 PC 켜짐
□ uvicorn 백엔드 실행됨 (터미널 창 열려있음)
□ 태블릿 Wi-Fi = 수집 PC Wi-Fi (같은 네트워크)
□ nav-app 우하단 패널 🟢 초록 확인
□ 프리셋 올바르게 선택되어 있음
□ ▶ 시작 누른 뒤 패널에 ● REC 배지 나타나는지 확인
```
